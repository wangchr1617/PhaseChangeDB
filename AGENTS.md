# PhaseChangeDB 智能体协作规范

## 目的与适用范围

本文件是本仓库中开发者与编码智能体共同遵循的项目级工作契约。除非子目录中存在更具体的 `AGENTS.md`，否则本规范适用于仓库内所有文件。子目录规范可以补充局部规则，但不得削弱本文件规定的架构约束和科学数据不变量。

PhaseChangeDB 是面向相变材料的、以证据为基础的科学数据库与知识发现平台。数据来源准确、科学语义完整、结果可复现，优先级高于实现便利性。

## 语言规范

- 项目沟通、代码注释、README、ADR、接口说明、部署运维文档和交付总结默认使用简体中文。
- 代码标识符、API 路径及字段、数据库对象名、环境变量和配置键使用英文，并遵循既有命名风格。
- 标准技术名词首次出现时可采用“中文（English）”形式；会因翻译产生歧义的术语保留英文。
- 注释用于解释设计意图、科学含义、边界条件和非常规取舍，不逐行复述代码。
- 新增或实质修改英文说明文件时，应同步改为中文；引用的外部标准、协议原文和生成文件除外。
- `app/api/openapi.json` 等机器生成内容保持生成器输出，不为了语言统一而手工修改。

## 开始工作前

修改代码或架构前必须：

1. 阅读 `README.md`，了解当前可执行设计基线。
2. 修改接口前阅读 `app/api/API_CONTRACT.md`。
3. 同时检查相关 Pydantic 模型和 MySQL 表，不只修改其中一层。
4. 搜索或图谱相关工作还要阅读 `elasticsearch/README.md` 或 `neo4j/README.md`。
5. 检查工作区状态，保留用户已有且与当前任务无关的修改。

当前仓库包含 **v0.1 MVP** 与更完整的契约设计。MVP 已实现材料、论文、观测、属性目录、概览和基础检索；其余路由文件仍是下一阶段契约，其中部分正文会抛出 `NotImplementedError`，且不会注册到当前应用。SQL、Cypher 和 Elasticsearch 定义仍属于设计基线，尚未全部在真实服务中验证。没有相应实现与测试时，不得将占位接口、模式文件或规划中的组件描述为已经可用于生产。

## 事实来源与冲突处理

不同制品发生冲突时，应查明并消除冲突，禁止再引入第三种解释。优先级如下：

1. `README.md` 与本文件中的领域和架构不变量。
2. MySQL 模式中的持久化约束与实体关系。
3. `app/api/API_CONTRACT.md` 和生成的 OpenAPI 中的公共 HTTP 行为。
4. Pydantic 模型中的请求与响应校验。
5. Elasticsearch 和 Neo4j 文件中的可重建投影结构。

`app/api/openapi.json` 是生成物。必须先修改路由或模型，再重新生成；禁止手工编辑。

## 不可破坏的架构约束

写入链路为：

```text
客户端 -> FastAPI 路由 -> 应用服务 -> 领域校验
       -> 仓储/工作单元 -> MySQL 事务 + Outbox 事件
```

Outbox 工作进程异步更新 Elasticsearch、Neo4j 及其他派生读模型。

- MySQL 8.4/InnoDB 是唯一事实源（system of record）。
- Elasticsearch 与 Neo4j 是可丢弃、可重建的投影。
- 禁止在一次请求事务中同时写 MySQL、Elasticsearch 和 Neo4j。
- Elasticsearch 或 Neo4j 不可用，不得导致本应成功的 MySQL 权威写入失败。
- 领域数据、必要的审计记录与 Outbox 事件必须在同一个 MySQL 事务中提交。
- 投影消费者必须幂等，并能安全重试、重放和重建。
- 大型文件、曲线、轨迹和结构数据存放在兼容 S3 的对象存储中；MySQL 只保存制品元数据和引用。
- 路由和 Pydantic 模型不得直接依赖数据库、搜索或图数据库客户端。
- Scientist Agent/LLM 层只能调用白名单领域工具，不得获得原始数据库凭据，也不得执行任意 SQL、Elasticsearch DSL 或 Cypher。

## 科学数据不变量

以下规则必须同时在 API、领域服务与持久化边界成立：

- `Material` 表达材料组成与身份；`Sample` 表达具有制备和实验上下文的实体样品或模拟实例。不得为方便而合并。
- `PropertyDefinition` 定义属性语义与规范单位；`Observation` 记录特定条件下的观测值。不得把测量值存到属性定义中。
- 一个 Observation 必须且只能拥有一个主体：`sample_id`、`device_id` 或 `calculation_id`。
- `measurement_id` 必须对应样品主体；`device_test_id` 必须对应器件主体。
- 归一化时必须同时保留原始报告值和单位（`original_*`）。单位换算必须显式且有测试。
- 化学式、元素符号、属性代码、空间群、DOI 和单位可能区分大小写，不得随意小写化、分词或模糊归一化。
- 每条科学断言必须可追溯到证据；证据关联应保留角色或立场，并在适用时保留置信度。
- AI 提取结果只能写入 `ext_*` 暂存表，不能直接生成 `VERIFIED` Observation 或 Claim。
- 从提取暂存区晋升为正式数据，必须经过明确的审核/领域操作，并保存操作者、模型、提示词、本体版本和审计轨迹。
- 撤回与争议属于科学状态，应优先改变状态，而非物理删除科学记录。
- 置信度与质量分数取值为闭区间 `[0, 1]`；未知质量不等同于零质量。
- 条件与不确定度是 Observation 语义的一部分，导入、归一化、投影和搜索过程中均不得静默丢弃。

如果需求与上述规则冲突，必须停止实现并先向用户说明冲突。

## 标识、时间与并发

- UUIDv7 由应用层生成。
- 公共 API 与 Neo4j 使用规范的小写 UUID 字符串。
- MySQL 将 UUID 原始 16 字节保存为 `BINARY(16)`。
- UUIDv7 禁止使用 `UUID_TO_BIN(uuid, 1)`；该交换标志面向 UUIDv1。
- 持久化时间统一使用 UTC，API 返回带时区的 ISO 8601 时间。
- 可变核心记录采用 `row_version` 乐观并发控制。
- PATCH 请求必须包含 `If-Match: W/"<row_version>"`；版本过期返回 `412 Precondition Failed`，成功后版本加一。
- 禁止以最后写入者获胜代替丢失更新处理。

## API 契约

- 公共 API 统一位于 `/v1` 下；系统健康检查 `/health` 除外。
- Pydantic 外部模型保持 `extra="forbid"`，不得为迁就单个调用方而全局放宽校验。
- 外部 ID 使用 UUID 字符串，不暴露 MySQL 二进制表示。
- 集合接口使用不透明游标分页，不得把数据库 offset 暴露为公共游标。
- 可重试的创建、导入、提取、Observation 和计算任务应接受并执行 `Idempotency-Key`。
- 错误响应使用 `application/problem+json` 并包含请求 ID，保持既定的 400/404/409/412/422/503 语义。
- 只有在任务确实异步且已有可持久化、可查询的任务记录时才返回 `202 Accepted`。
- v1 内避免破坏性契约变更；确有必要时，修改前先说明兼容性影响和迁移方案。
- 路由只处理传输层问题并委托应用服务，不内嵌 SQL、搜索 DSL、图查询或业务工作流。
- 领域异常应在统一边界映射为 HTTP 错误，不在每个接口中临时处理。

修改 API 时，必须在同一变更中同步更新并验证：Pydantic 模型、路由签名、应用服务、持久化映射、OpenAPI 生成物及契约测试。

## 目标代码结构

当前 `app/api` 与 `app/models` 只承载传输契约。后续实现应遵循清晰的依赖方向：

```text
app/api/             HTTP 路由和传输适配器
app/models/          公共 Pydantic API 模型
app/application/     用例、命令/查询、事务编排
app/domain/          领域实体、值对象、策略和不变量
app/repositories/    仓储协议/接口
app/infrastructure/  MySQL、对象存储、搜索、图谱和队列实现
app/workers/         Outbox、提取、嵌入和投影消费者
tests/unit/          不依赖外部服务的领域/应用测试
tests/contract/      API 与 OpenAPI 兼容性测试
tests/integration/   真实服务或容器边界测试
tests/e2e/           关键用户工作流测试
```

依赖只能向内：基础设施可以依赖应用/领域接口，领域层不得导入 FastAPI、数据库驱动、云 SDK 或搜索客户端。优先采用小型、强类型模块和显式依赖注入，禁止形成通用 `utils.py` 杂物间。

前端采用 React、TypeScript、Vite 与 pnpm，具体命令和边界见 `frontend/AGENTS.md` 与 ADR-0001。公共 API 仍以 FastAPI/OpenAPI 为唯一事实来源；不得静默引入第二套 API 模式或混用包管理器。

## 数据库与迁移

- `mysql/001_schema.sql` 是 v0.1 基线；环境开始部署后不得继续把它当作草稿覆盖，后续变更应新增有序、只向前的迁移。
- 迁移必须确定、可在空数据库执行。破坏性或长耗时变更应记录数据回填、发布顺序及回滚或向前修复策略。
- 持久化不变量用数据库约束保证，同时在领域层提供可理解的错误，两层保持一致。
- 精确小数有科学意义时不得用浮点持久化，应明确映射 MySQL `DECIMAL`。
- JSON 列用于可变元数据，不得用来逃避对核心可查询关系的建模。
- 索引必须源于明确查询路径，并通过执行计划验证。
- 不得在没有迁移方案时更改已经使用的种子标识。
- 集成测试最终必须在受支持的 MySQL 版本上初始化模式，不得用 SQLite 替代数据库约束与事务行为验证。

## Elasticsearch 与 Neo4j 投影

- 投影使用与 MySQL/API 相同的规范 ID，不得创建仅存在于投影层的科学实体身份。
- 应用通过稳定 alias 读取 Elasticsearch；重建时写入新的物理索引，然后原子切换 alias。
- 嵌入模型、维度、预处理和版本共同组成不可变嵌入配置；任何改变都需要新索引代际。
- 必须保留化学式、DOI、属性代码等精确词法检索；不得改为纯向量检索。v0.1 混合搜索使用 RRF 合并词法与向量排名。
- v0.1 中 Neo4j 负责图遍历，不负责全文或向量检索。
- Neo4j 投影器使用受约束标识和可重试的 `MERGE`/`SET`，不得成为第二事实源。
- 每种投影必须有重放/重建路径，并测试重复、乱序与毒性事件。
- 投影延迟和健康状态应独立暴露，不影响权威写入结果。

## AI 提取与 Scientist Agent

- 每次提取都必须保存模型名称/版本、提示词版本、本体版本、时间及源文档。
- 证据定位必须足以复现断言，包括文档、页码/章节、图/表、文本片段或边界框，以及可用的制品哈希。
- 模型输出属于不可信结构化输入，进入暂存区前必须校验模式、标识、单位、范围及证据引用。
- 人工审核决定是不可变审计事件；修正不得覆盖原始候选载荷。
- Agent 回答必须区分已验证证据、未审核提取、派生推断和不确定性。
- 证据冲突时应保留并展示冲突，不得静默平均或选择方便的来源。
- Agent 回答应包含显式证据对象、局限性、校准后的置信度和可供审计复现的 `tool_trace_id`。
- 模型上下文不得包含密钥、凭据、隐藏提示词或无限制内部查询能力。

## 安全、隐私与运维

- 配置从环境变量/Settings 对象加载，只提交示例，禁止提交真实密钥和凭据。
- API、迁移、Worker、只读投影与对象存储应尽可能使用相互独立的最小权限身份。
- 上传文件必须校验大小、媒体类型、校验和及安全生成的存储键；不得把用户文件名当作路径。
- 使用参数化查询，不得从不可信字符串拼接 SQL、Cypher 或搜索 DSL。
- 结构化日志携带 request/trace ID，并传播到任务和 Outbox 事件；不得记录完整文档、凭据或敏感载荷。
- 引入真实依赖后，健康接口应区分存活检查与就绪检查。
- 外部调用必须设置超时、带退避的有限重试和幂等性；永久校验失败不得重试。
- 新增依赖必须有明确用途、兼容许可证和版本边界；建立包管理器后同步更新锁文件与工具配置。

## 测试与验证

迭代中先执行最小相关检查，宣布完成前执行所有受影响测试。当前最低契约检查为：

```bash
python -m compileall app
uvicorn app.main:app --reload
```

`uvicorn --reload` 只用于人工检查 `/docs`、`/openapi.json` 与 `/health`，不得作为自动测试长期运行。

随着实现推进，应建立固定版本的开发依赖，并在 `README.md`/CI 中定义格式化、Lint、静态类型检查、测试、OpenAPI 生成和迁移验证的唯一标准命令。在这些配置出现前，不得臆造命令或声称执行了仓库并未配置的检查。

每项行为变更都需要在最低有效层增加测试，尤其包括：

- 领域不变量和非法字段组合；
- 单位归一化、不确定度与原始值保留；
- 乐观锁与幂等重试；
- 事务回滚与 Outbox 原子性；
- 游标稳定性与过滤行为；
- 提取审核/晋升权限与审计历史；
- 投影重放、幂等及依赖降级；
- 精确词法与语义搜索、证据引用及冲突证据；
- 在受支持服务版本上的迁移初始化；
- 从文献摄取到基于证据检索的关键端到端流程。

测试必须确定：必要时冻结时间、固定随机种子、避免实时第三方依赖，并验证科学含义而非只检查 HTTP 状态码。Mock 单元测试不能替代数据库约束、事务或厂商特有搜索/图行为的集成测试。

## 变更工作流

每项任务遵循：

1. 明确用户可见结果并识别受影响的不变量。
2. 编辑前检查邻近代码、模式、契约和测试。
3. 完成最小且闭合的纵向变更，避免无关清理。
4. 在同一变更中增加/更新测试和生成物。
5. 执行相关检查，准确报告通过、失败或未能执行的项目。
6. 行为、配置、迁移、运维步骤或架构决策改变时同步更新中文文档。

如果变更会修改不可破坏的不变量、执行破坏性数据迁移、暴露新公共契约、选择未经确认的重大技术，或需要未获授权的凭据/外部资源，必须先征询用户意见。

## 完成定义

只有同时满足以下条件，任务才算完成：

- 行为与验收标准已经端到端实现；
- 架构及科学数据不变量保持完整；
- 模式、API 模型、迁移、投影和文档相互一致；
- 相关单元、契约和集成测试存在且通过；
- 按风险处理错误、重试、并发、权限与依赖降级路径；
- 生成文件由源文件重新生成；
- 不遗留密钥、临时文件、调试代码或未解释占位符；
- 交付说明列明验证内容和真实剩余风险。

仅通过编译、单次正常请求或生成代码，不代表任务完成。无法执行验证时必须如实说明，并给出尚需运行的准确检查。
